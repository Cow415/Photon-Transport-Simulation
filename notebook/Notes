Initial Pipeline:
RI data unpack
   ↓
Geometry / Volume
   ↓
iso2mesh (tetgen / cgalmesh)
   ↓
nodes + elements + region labels
   ↓
MCX (.json or MATLAB struct)
   ↓
Photon Monte Carlo simulation



=========== MatLab ==========
in command window:
    accessing script require addpath('') command

    prefixing "!" before command to run bash instead of matlab