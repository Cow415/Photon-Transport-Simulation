2D phantom
   ↓
extrude → 3D volume
   ↓
pyiso2mesh → tetrahedral mesh(Not required for 2D padding, though lose accuracy in propagation)
   ↓
MCX config(Must have NVIDIA Driver -> I hate nvidia i hope they go out of business)
   ↓
MCX simulation()
   ↓
fluence / photon data

Architecture:
project/
│
├── environment.yml
├── data/
│   └── phantom.npy           # your 2D matrix (3 labels)
│
├── mesh/simulation
│   └── generate_mesh.py      # pyiso2mesh pipeline
│   └── run_mcx.py            # MCX configuration + run
│
└── output/
    ├── mesh/
    └── mcx/